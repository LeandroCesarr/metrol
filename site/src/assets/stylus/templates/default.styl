.t-default {
  height: 100%;
  position: relative;
  padding-left: 40px;
}

.t-default_menu {
  position: fixed;
  top: 0;
  bottom: 0;
  z-index: 10;
  right: calc(100% - 40px);
}

.t-default_ctn {
  padding: 20px;
}